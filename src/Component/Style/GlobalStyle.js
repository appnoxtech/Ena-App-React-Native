import { StyleSheet} from 'react-native'
import { white } from '../DesignTheme'

const GlobalStyle = StyleSheet.create({
    container:{
        flex:1,
        backgroundColor:white
    },

})

export default GlobalStyle